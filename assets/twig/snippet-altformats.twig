
        <div class="alt-formats">
          <form id="alternate_mime_options" action="" method="GET">
            {% for long,short in alternate_formats %}
              <a href="#" title="Preview as {{long}}" data-mime="{{long}}" class="label alternate_format">{{short}}</a>
            {% endfor %}
          </form>
        </div>
